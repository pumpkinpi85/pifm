# Optional controls

See **[../HARDWARE.md](../HARDWARE.md)**.

When `gpio_enabled` is true (default **false**):

- LED on `led_pin` (default BCM 18)
- Shutdown switch on `switch_pin` (default BCM 5)
